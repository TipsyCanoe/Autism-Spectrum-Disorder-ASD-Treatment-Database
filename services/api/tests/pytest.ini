[pytest]
markers =
    unit: unit tests that don't require a database connection
    integration: tests that interact with the database
    api: tests that make HTTP requests to the API
    e2e: end-to-end tests that simulate complete user workflows
    performance: tests that measure and verify performance characteristics
